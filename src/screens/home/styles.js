import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  subtitle: {
    fontSize: 20,
    color: "#000000",
    marginTop: 40,
  },
  row: {
    flexDirection: "row",
    flexWrap: "wrap",
  },
});

export default styles;
